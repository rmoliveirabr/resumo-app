<header>
  <div>
    <nav>
      <% if logged_in? %>
        Olá <%= current_user.full_name %> |
        <%= link_to "Perfil", current_user %> |
        <%= link_to "Settings", edit_user_path(current_user) %> |
        <%= link_to "Log out", sign_out_path, method: "delete" %><br/><br/>
      <% else %>
        <%= link_to "Log in", sign_in_path %> |
      <% end %>

      <% if logged_in? %>
        <br/><br/>
        <%= link_to "Criar resumo", new_post_path(current_user) %> |
        <%= link_to "Meus resumos", posts_path(current_user) %> |
      <% end %>

      <%= link_to "Listar resumos", posts_path %>

      <br/><br/>

      <%= form_tag(posts_path, method: 'GET', class: 'navbar-form navbar-left') do %>
        <div class="input-group">
          Buscar resumos:
          <%= search_field_tag :search, params[:search], placeholder: "Buscar", class: "form-control" %>
          <%= button_tag "", :class => 'glyphicon glyphicon-search',:name => nil%>
        </div>
      <% end %>

      <br/><br/>

    </nav>
  </div>
</header>
<hr/>
